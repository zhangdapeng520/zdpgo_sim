public class Puppy{
   int age222;
   public Puppy(String name){
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      // 这个构造器仅有一个参数：name
      System.out.println("小狗的名字是 : " + name );
   }

   public void setAge(int age) {
      age222 = age;
   }

   // 这个变量名被修改了
   public int getAge1111111() {
      System.out.println("小狗的年龄为 : " + age222);
      return age222;
   }
   
   // 下面的两个代码是新增的
   public void show() {
      System.out.println("小狗的年龄为 : " + age222);
   }

   public void info() {
      System.out.println("小狗的年龄为 : " + age222);
      System.out.println("小狗的年龄为 : " + age222);
      System.out.println("小狗的年龄为 : " + age222);
      System.out.println("小狗的年龄为 : " + age222);
      System.out.println("小狗的年龄为 : " + age222);
   }

   public static void main(String[] args){
      /* 创建对象 */
      Puppy myPuppy = new Puppy( "tommy" );
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      /* 通过方法来设定age */
      myPuppy.setAge( 2 );
      /* 调用另一个方法获取age */
      myPuppy.getAge( );
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      /*你也可以像下面这样访问成员变量 */
      System.out.println("变量值 : " + myPuppy.age222 );
   }
}